import 'package:flutter/material.dart';

class ThemeConstant {
  ThemeConstant._();

  static const Color darkPrimaryColor = Color(0xFF00796B); // Deep Teal
  static const Color primaryColor = Color(0xFF0288D1); // Blue
  static const Color appBarColor =
      Color(0xFF00796B); // Matching Teal for consistency
  static const Color accentColor = Color(0xFFFFFFFF); // White for contrast
}
